import React from 'react';
import './GreenBar.css';

const GreenBar = () => {
  return (
    <div className="green-bar">
      {/* Green horizontal bar - no content needed, just visual element */}
    </div>
  );
};

export default GreenBar;
